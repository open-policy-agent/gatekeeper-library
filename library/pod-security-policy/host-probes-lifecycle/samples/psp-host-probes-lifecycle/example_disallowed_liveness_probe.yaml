apiVersion: v1
kind: Pod
metadata:
  name: nginx-liveness-probe-host-disallowed
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: nginx
    livenessProbe:
      httpGet:
        path: /
        port: 80
        host: 127.0.0.1
