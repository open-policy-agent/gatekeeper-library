apiVersion: v1
kind: Pod
metadata:
  name: nginx-exempt-sysctls
  labels:
    app: nginx-exempt-sysctls
spec:
  containers:
    - name: nginx
      image: nginx
  securityContext:
    sysctls:
      - name: kernel.shm_rmid_forced
        value: "1"
