apiVersion: constraints.gatekeeper.sh/v1beta1
kind: K8sRequiredProbeTimeoutSeconds
metadata:
  name: must-have-timeout-seconds-field-in-probes
spec:
  match:
    kinds:
      - apiGroups: [""]
        kinds: ["Pod"]
  parameters:
    probes: ["readinessProbe", "livenessProbe"]
