apiVersion: v1
kind: Pod
metadata:
  name: gpu-pod-exceeds-limit
spec:
  containers:
    - name: training
      image: myrepo/large-training:v1
      resources:
        limits:
          nvidia.com/gpu: "8"
