apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: team-b-service
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: appservice
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007
