apiVersion: v1
kind: Service
metadata:
  name: app-service
  namespace: team-a-service
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: appservice
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007
