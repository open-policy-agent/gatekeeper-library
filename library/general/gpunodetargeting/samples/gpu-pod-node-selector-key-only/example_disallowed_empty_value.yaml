apiVersion: v1
kind: Pod
metadata:
  name: gpu-pod-node-selector-empty-value
spec:
  nodeSelector:
    nvidia.com/gpu.present: ""
  containers:
    - name: training
      image: nvidia/cuda:12.0-runtime
      resources:
        limits:
          nvidia.com/gpu: "1"