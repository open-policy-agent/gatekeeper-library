apiVersion: v1
kind: Pod
metadata:
  name: gpu-pod-gpu-request-missing
spec:
  containers:
    - name: training
      image: nvidia/cuda:12.0-runtime
      resources:
        requests:
          memory: "16Gi"
          cpu: "2"
        limits:
          nvidia.com/gpu: "1"
          memory: "16Gi"
          cpu: "4"