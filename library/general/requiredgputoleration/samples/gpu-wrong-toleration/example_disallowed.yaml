apiVersion: v1
kind: Pod
metadata:
  name: gpu-wrong-toleration
spec:
  containers:
    - name: training
      image: nvidia/cuda:12.0-runtime
      resources:
        limits:
          nvidia.com/gpu: "1"
  tolerations:
    - key: "workload"
      operator: "Exists"
      effect: "NoSchedule"