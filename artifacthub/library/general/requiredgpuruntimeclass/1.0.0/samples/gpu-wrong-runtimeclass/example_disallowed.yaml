apiVersion: v1
kind: Pod
metadata:
  name: gpu-wrong-runtimeclass
spec:
  runtimeClassName: runc
  containers:
    - name: training
      image: nvidia/cuda:12.0-runtime
      resources:
        limits:
          nvidia.com/gpu: "1"