apiVersion: v1
kind: Pod
metadata:
  name: gpu-job-with-deadline
spec:
  activeDeadlineSeconds: 3600
  containers:
    - name: training
      image: nvidia/cuda:12.0-runtime
      resources:
        limits:
          nvidia.com/gpu: "1"
