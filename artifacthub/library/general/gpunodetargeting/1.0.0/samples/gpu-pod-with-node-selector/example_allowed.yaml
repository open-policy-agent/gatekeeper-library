apiVersion: v1
kind: Pod
metadata:
  name: gpu-pod-with-node-selector
spec:
  nodeSelector:
    nvidia.com/gpu.present: "true"
  containers:
    - name: training
      image: nvidia/cuda:12.0-runtime
      resources:
        limits:
          nvidia.com/gpu: "1"