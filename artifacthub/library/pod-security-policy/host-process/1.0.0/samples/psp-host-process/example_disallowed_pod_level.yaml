apiVersion: v1
kind: Pod
metadata:
  name: nginx-host-process-disallowed
  labels:
    app: nginx-host-process
spec:
  securityContext:
    windowsOptions:
      hostProcess: true
  hostNetwork: true
  containers:
  - name: nginx
    image: nginx
