apiVersion: v1
kind: Pod
metadata:
  name: nginx-probe-host-allowed-exempt
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: safeimages.com/nginx
    livenessProbe:
      httpGet:
        path: /
        port: 80
        host: 127.0.0.1
