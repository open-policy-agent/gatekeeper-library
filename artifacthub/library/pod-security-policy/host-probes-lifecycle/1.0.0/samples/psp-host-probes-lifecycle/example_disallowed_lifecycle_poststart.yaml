apiVersion: v1
kind: Pod
metadata:
  name: nginx-lifecycle-poststart-host-disallowed
  labels:
    app: nginx
spec:
  containers:
  - name: nginx
    image: nginx
    lifecycle:
      postStart:
        httpGet:
          path: /
          port: 80
          host: 127.0.0.1
