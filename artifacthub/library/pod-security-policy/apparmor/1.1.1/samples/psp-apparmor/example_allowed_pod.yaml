apiVersion: v1
kind: Pod
metadata:
  name: nginx-apparmor-allowed
  labels:
    app: nginx-apparmor
spec:
  securityContext:
    appArmorProfile:
      type: "RuntimeDefault"
  containers:
  - name: nginx
    image: nginx
